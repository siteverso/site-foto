---
import { getMessages, normalizeLocale } from '../i18n';
const locale = normalizeLocale(Astro.cookies.get('fotolife-language')?.value);
const t = getMessages(locale);
---
<section class="composer panel" aria-labelledby="composer-title">
  <div class="avatar" data-user-avatar>MU</div>
  <form data-composer>
    <label id="composer-title" for="murmur-text">{t.feed.composerLabel}</label>
    <textarea id="murmur-text" name="text" maxlength="256" placeholder={t.feed.composerPlaceholder} required></textarea>
    <div class="composer-footer">
      <div class="murmur-progress" data-murmur-progress aria-label={t.common.characterLimit.replace('{percent}', '0').replace('{limit}', '256')}>
        <span class="murmur-progress-track"><span class="murmur-progress-fill" data-progress-fill></span></span>
        <span class="murmur-progress-value" data-progress-value>0%</span>
      </div>
      <button class="button primary" type="submit">{t.feed.publish}</button>
    </div>
  </form>
</section>
